# random-python

A sandbox for quick Python scripts and algorithm experiments.

## Setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -e ".[dev]"
```

## Usage

```bash
python scripts/monte_carlo_pi.py
```

## Tests

```bash
pytest tests/ -v
```
